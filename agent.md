# agent.md

`AGENTS.md` is the canonical Codex instruction file and should be read automatically by Codex.

This lowercase file exists because the project owner requested `agent.md`. Its instructions are identical in intent: **read and obey `AGENTS.md` before doing any work.**
